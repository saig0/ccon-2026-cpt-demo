camunda:
  process-test:
    runtime-mode: shared

logging:
  level:
    root: warn
    io:
      camunda:
        demo: debug
        process:
          test: info
      # Hide default logging from Testcontainers
    org:
      testcontainers: warn
    tc: error
